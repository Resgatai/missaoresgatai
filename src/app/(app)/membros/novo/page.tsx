import Link from "next/link";
import { createMember } from "../actions";
import { MaskedInput } from "@/components/masked-input";

export default async function NewMemberPage({ searchParams }: { searchParams: Promise<{ erro?: string }> }) { const { erro } = await searchParams; const field = "h-11 rounded-lg border border-slate-300 px-3 font-normal"; return <main className="mx-auto max-w-4xl p-4 sm:p-7 lg:p-9"><Link href="/membros" className="text-sm font-bold text-[#7b4b2a]">← Voltar para membros</Link><h1 className="mt-5 text-2xl font-bold text-slate-800">Cadastrar membro</h1><p className="mt-1 text-sm text-slate-500">O celular será usado como WhatsApp. Dados sensíveis são protegidos.</p>{erro && <p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-800">{erro}</p>}<form action={createMember} className="mt-5 space-y-5"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="grid gap-4 sm:grid-cols-2"><label className="grid gap-1 text-xs font-bold text-slate-700 sm:col-span-2">Nome completo *<input required name="fullName" className={field}/></label><label className="grid gap-1 text-xs font-bold text-slate-700">E-mail<input name="email" type="email" className={field}/></label><label className="grid gap-1 text-xs font-bold text-slate-700">Celular / WhatsApp<MaskedInput name="mobilePhone" kind="phone" placeholder="(85) 99999-9999" className={field}/></label><label className="grid gap-1 text-xs font-bold text-slate-700">CPF<MaskedInput name="cpf" kind="cpf" placeholder="000.000.000-00" className={field}/></label><label className="grid gap-1 text-xs font-bold text-slate-700">Nascimento<input name="birthDate" type="date" className={field}/></label><label className="grid gap-1 text-xs font-bold text-slate-700">Cidade<input name="city" className={field}/></label><label className="grid gap-1 text-xs font-bold text-slate-700">UF<input name="state" maxLength={2} className={`${field} uppercase`}/></label><label className="grid gap-1 text-xs font-bold text-slate-700">Situação<select name="status" defaultValue="visitor" className={field}><option value="visitor">Visitante</option><option value="new_convert">Novo convertido</option><option value="congregant">Congregado</option><option value="active">Membro ativo</option><option value="follow_up">Em acompanhamento</option></select></label><label className="grid gap-1 text-xs font-bold text-slate-700 sm:col-span-2">Observações<textarea name="notes" rows={4} className="rounded-lg border border-slate-300 p-3 font-normal"/></label></div></section><button className="rounded-lg bg-[#7b4b2a] px-5 py-3 text-sm font-bold text-white">Salvar membro</button></form></main>; }
