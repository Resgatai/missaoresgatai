import Link from "next/link";
import { ImageUpload } from "@/components/image-upload";
import { PushActivationButton } from "@/components/push-activation-button";
import { updateChurchSettings } from "../mvp/actions";
import { requirePermission } from "@/lib/auth/authorization";
import { getDb } from "@/lib/db";
import { churchSettings } from "@/lib/db/schema";
export default async function SettingsPage({ searchParams }: { searchParams: Promise<{ salvo?: string; erro?: string }> }) { await requirePermission("configuracoes.gerenciar"); const message = await searchParams; const setting = (await getDb().select().from(churchSettings).limit(1))[0]; return <main className="mx-auto max-w-5xl p-4 sm:p-7 lg:p-9"><header className="mb-6"><p className="text-xs font-bold uppercase tracking-widest text-[#7b4b2a]">Administração</p><h1 className="mt-1 text-2xl font-bold text-slate-800">Configurações da igreja</h1></header>{message.salvo && <p className="mb-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800">Configurações salvas.</p>}{message.erro && <p className="mb-4 rounded-lg bg-red-50 p-3 text-sm text-red-800">{message.erro}</p>}<div className="grid gap-5 lg:grid-cols-[1fr_300px]"><form action={updateChurchSettings} className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="grid gap-4 sm:grid-cols-2"><label className="grid gap-1 text-xs font-bold sm:col-span-2">Nome da igreja<input name="churchName" required defaultValue={setting?.churchName || "Missão Resgatai"} className="h-10 rounded border border-slate-300 px-3 font-normal"/></label><label className="grid gap-1 text-xs font-bold sm:col-span-2">Slogan<input name="slogan" defaultValue={setting?.slogan || ""} className="h-10 rounded border border-slate-300 px-3 font-normal"/></label><label className="grid gap-1 text-xs font-bold sm:col-span-2">Endereço<input name="address" defaultValue={setting?.address || ""} className="h-10 rounded border border-slate-300 px-3 font-normal"/></label><label className="grid gap-1 text-xs font-bold">Telefone<input name="phone" defaultValue={setting?.phone || ""} className="h-10 rounded border border-slate-300 px-3 font-normal"/></label><label className="grid gap-1 text-xs font-bold">E-mail<input name="email" type="email" defaultValue={setting?.email || ""} className="h-10 rounded border border-slate-300 px-3 font-normal"/></label><label className="grid gap-1 text-xs font-bold">Instagram<input name="instagramUrl" type="url" defaultValue={setting?.instagramUrl || ""} className="h-10 rounded border border-slate-300 px-3 font-normal"/></label><label className="grid gap-1 text-xs font-bold">YouTube<input name="youtubeUrl" type="url" defaultValue={setting?.youtubeUrl || ""} className="h-10 rounded border border-slate-300 px-3 font-normal"/></label><label className="grid gap-1 text-xs font-bold sm:col-span-2">Logotipo<ImageUpload name="logoUrl" scope="logo" currentValue={setting?.logoUrl || ""}/></label></div><button className="mt-6 rounded-lg bg-[#7b4b2a] px-5 py-3 text-sm font-bold text-white">Salvar configurações</button></form><aside className="space-y-4"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold text-slate-800">Notificações</h2><p className="mt-1 text-sm text-slate-500">Ative neste navegador para receber eventos, escalas, avisos e pendências.</p><div className="mt-3"><PushActivationButton/></div></section><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-bold text-slate-800">Contas e cargos</h2><Link href="/usuarios" className="mt-3 inline-block text-sm font-bold text-[#7b4b2a]">Gerenciar usuários</Link></section></aside></div></main>; }
