import Link from "next/link";
import { createPrayerRequest } from "../(app)/mvp/actions";

export default async function PublicPrayerPage({ searchParams }: { searchParams: Promise<{ enviado?: string; erro?: string }> }) { const message = await searchParams; return <main className="grid min-h-screen place-items-center bg-slate-50 p-5"><section className="w-full max-w-xl rounded-2xl border border-slate-200 bg-white p-7 shadow-sm"><Link href="/" className="text-sm font-bold text-[#1769aa]">← Voltar ao início</Link><p className="mt-6 text-xs font-bold uppercase tracking-widest text-[#1769aa]">Cuidado e intercessão</p><h1 className="mt-2 text-3xl font-bold text-slate-800">Envie seu pedido de oração</h1><p className="mt-2 text-sm leading-6 text-slate-500">Compartilhe apenas o que se sentir à vontade. Você pode marcar o pedido como confidencial.</p>{message.enviado && <p className="mt-5 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800">Recebemos seu pedido. Nossa equipe estará em oração.</p>}{message.erro && <p className="mt-5 rounded-lg bg-red-50 p-3 text-sm text-red-800">{message.erro}</p>}<form action={createPrayerRequest} className="mt-6 space-y-4"><input type="hidden" name="returnTo" value="/pedido-de-oracao"/><label className="grid gap-1.5 text-xs font-bold text-slate-700">Nome (opcional)<input name="requesterName" className="h-10 rounded-lg border border-slate-300 px-3 font-normal"/></label><label className="grid gap-1.5 text-xs font-bold text-slate-700">E-mail (opcional)<input name="requesterEmail" type="email" className="h-10 rounded-lg border border-slate-300 px-3 font-normal"/></label><label className="grid gap-1.5 text-xs font-bold text-slate-700">Pedido de oração<textarea name="body" required rows={6} className="rounded-lg border border-slate-300 p-3 font-normal"/></label><label className="flex items-center gap-2 text-sm text-slate-700"><input name="confidential" type="checkbox" value="yes"/>Este pedido é confidencial</label><button className="w-full rounded-lg bg-[#1769aa] py-3 text-sm font-bold text-white">Enviar pedido</button></form></section></main>; }
