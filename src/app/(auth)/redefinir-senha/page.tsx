import Link from "next/link";
import { resetPassword } from "../recuperar-senha/actions";

export default async function NewPassword({ searchParams }: { searchParams: Promise<{ token?: string; erro?: string }> }) {
  const { token, erro } = await searchParams;
  if (!token) return <main className="grid min-h-screen place-items-center bg-slate-50 p-5"><section className="w-full max-w-md rounded-2xl border border-slate-200 bg-white p-7 text-center shadow-sm"><h1 className="text-2xl font-bold text-slate-800">Link inválido</h1><Link href="/recuperar-senha" className="mt-5 inline-block text-sm font-bold text-[#1769aa]">Solicitar novo link</Link></section></main>;
  return <main className="grid min-h-screen place-items-center bg-slate-50 p-5"><section className="w-full max-w-md rounded-2xl border border-slate-200 bg-white p-7 shadow-sm"><p className="text-xs font-bold uppercase tracking-widest text-[#1769aa]">Segurança da conta</p><h1 className="mt-3 text-3xl font-bold tracking-tight text-slate-800">Defina uma nova senha</h1><p className="mt-2 text-sm text-slate-500">Use pelo menos 8 caracteres e não reutilize uma senha antiga.</p>{erro && <p className="mt-5 rounded-lg bg-red-50 p-3 text-sm text-red-800">{erro}</p>}<form action={resetPassword} className="mt-6 space-y-4"><input type="hidden" name="token" value={token}/><label className="grid gap-2 text-xs font-bold text-slate-700">Nova senha<input name="password" type="password" minLength={8} required autoComplete="new-password" className="h-11 rounded-lg border border-slate-300 px-3 font-normal"/></label><label className="grid gap-2 text-xs font-bold text-slate-700">Confirmar nova senha<input name="confirmation" type="password" minLength={8} required autoComplete="new-password" className="h-11 rounded-lg border border-slate-300 px-3 font-normal"/></label><button className="w-full rounded-lg bg-[#1769aa] py-3 text-sm font-bold text-white">Salvar nova senha</button></form></section></main>;
}
