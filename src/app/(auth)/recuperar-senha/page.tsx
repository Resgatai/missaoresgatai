import Link from "next/link";
import { requestPasswordReset } from "./actions";

export default async function ResetPasswordRequest({ searchParams }: { searchParams: Promise<{ enviado?: string; erro?: string }> }) {
  const message = await searchParams;
  return <main className="grid min-h-screen place-items-center bg-slate-50 p-5"><section className="w-full max-w-md rounded-2xl border border-slate-200 bg-white p-7 shadow-sm"><p className="text-xs font-bold uppercase tracking-widest text-[#1769aa]">Acesso à plataforma</p><h1 className="mt-3 text-3xl font-bold tracking-tight text-slate-800">Recupere seu acesso</h1><p className="mt-2 text-sm leading-6 text-slate-500">Informe o e-mail da sua conta. Se ele estiver cadastrado, enviaremos um link seguro para criar uma nova senha.</p>{message.enviado && <p className="mt-5 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-800">Se houver uma conta com este e-mail, o link de recuperação foi enviado.</p>}{message.erro && <p className="mt-5 rounded-lg bg-red-50 p-3 text-sm text-red-800">{message.erro}</p>}<form action={requestPasswordReset} className="mt-6 space-y-4"><label className="grid gap-2 text-xs font-bold text-slate-700">E-mail<input name="email" type="email" required autoComplete="email" className="h-11 rounded-lg border border-slate-300 px-3 font-normal outline-none focus:border-[#1769aa]" placeholder="voce@igreja.org"/></label><button className="w-full rounded-lg bg-[#1769aa] py-3 text-sm font-bold text-white">Enviar link de recuperação</button></form><Link className="mt-5 block text-center text-xs font-bold text-[#1769aa]" href="/login">Voltar para entrar</Link></section></main>;
}
