"use client";
import { useState } from "react";
export function FinancialForm({ accounts, categories, action }: { accounts: { id: string; name: string }[]; categories: { id: string; name: string; type: "income" | "expense" }[]; action: (formData: FormData) => void | Promise<void> }) { const [type, setType] = useState<"income" | "expense">("income"); const filtered = categories.filter((category) => category.type === type); return <form action={action} className="space-y-5"><section className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><div className="grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-xs font-bold text-slate-700">Tipo<select name="type" value={type} onChange={(event) => setType(event.target.value as "income" | "expense")} className="h-11 rounded-lg border border-slate-300 px-3 font-normal"><option value="income">Entrada</option><option value="expense">Saída</option></select></label><label className="grid gap-2 text-xs font-bold text-slate-700">Valor (R$)<input name="amount" required inputMode="decimal" className="h-11 rounded-lg border border-slate-300 px-3 font-normal" placeholder="0,00"/></label><label className="grid gap-2 text-xs font-bold text-slate-700 sm:col-span-2">Descrição<input name="description" required maxLength={500} className="h-11 rounded-lg border border-slate-300 px-3 font-normal" placeholder="Resumo administrativo do lançamento"/></label><label className="grid gap-2 text-xs font-bold text-slate-700">Conta<select name="accountId" required className="h-11 rounded-lg border border-slate-300 px-3 font-normal">{accounts.map((account) => <option key={account.id} value={account.id}>{account.name}</option>)}</select></label><label className="grid gap-2 text-xs font-bold text-slate-700">Categoria<select name="categoryId" required className="h-11 rounded-lg border border-slate-300 px-3 font-normal" key={type}>{filtered.length ? filtered.map((category) => <option key={category.id} value={category.id}>{category.name}</option>) : <option value="">Cadastre uma categoria</option>}</select></label><label className="grid gap-2 text-xs font-bold text-slate-700">Data e horário<input name="occurredAt" type="datetime-local" required defaultValue={new Date().toISOString().slice(0, 16)} className="h-11 rounded-lg border border-slate-300 px-3 font-normal"/></label></div></section><div className="flex justify-end gap-3"><button className="rounded-lg bg-[#7b4b2a] px-4 py-3 text-sm font-bold text-white">Criar lançamento</button></div></form>; }
