"use client";
import { Bar, BarChart, CartesianGrid, Legend, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
export function FinancialChart({ data }: { data: { month: string; entradas: number; saidas: number }[] }) { return <div className="h-72"><ResponsiveContainer width="100%" height="100%"><BarChart data={data}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="month"/><YAxis tickFormatter={(value) => `R$ ${value}`}/><Tooltip formatter={(value) => new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" }).format(Number(value))}/><Legend/><Bar dataKey="entradas" fill="#cdb019" radius={[4,4,0,0]}/><Bar dataKey="saidas" fill="#7b4b2a" radius={[4,4,0,0]}/></BarChart></ResponsiveContainer></div>; }
